import SwiftUI

extension Font {
    /// Inter Thin - used for "red cocoa" / "Red Cocoa" brand text
    static func interThin(size: CGFloat) -> Font {
        .custom("Inter-Thin", size: size)
    }
}
